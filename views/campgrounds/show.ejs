<% include ../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-md-3">
            <p class="lead">YelpCamp</p>
            <div class="list-group">
                <li class="list-group-item active">Info-1</li>
                <li class="list-group-item">Info-2</li>
                <li class="list-group-item">Info-3</li>
            </div>
        </div>
        <div class="col-md-9">
            <div class="card">
                <img class="card-img-top image-fluid" src="<%= campground.image %>">
                <div class="card-body p-2">
                    <h4 class="float-right">$<%= campground.price %>/night</h4>
                    <h4 class="card-link">
                        <a href="#"><h4><%= campground.name %></h4></a>
                    </h4>
                    <p class="card-text"><%= campground.description %></p>
                    <p class="card-text small">
                        <em>Campground submitted by: <%= campground.author.username %>, <%= moment(campground.createdAt).fromNow() %></em>
                    </p>
                    <% if (currentUser && campground.author.id.equals(currentUser._id)) { %>
                        <a href="/campgrounds/<%= campground.slug %>/edit" class="btn btn-sm btn-warning">
                            Edit Campground
                        </a>
                        <form class="deleteForm" action="/campgrounds/<%= campground.slug %>?_method=DELETE" method="POST">
                            <button class="btn btn-sm btn-danger">Delete Campground </button>
                        </form>
                    <% } %>
                </div>
            </div>
            <div class="card bg-light mt-4 p-2">
                <div class="text-right">
                    <a href="/campgrounds/<%= campground.slug %>/comments/new" class="btn btn-primary">Add a new comment</a>
                </div>
                <hr>
                <% campground.comments.forEach((comment) => { %>
                    <div class="row">
                        <div class="col-md-12">
                            <strong><%= comment.author.username %></strong>
                             <% if (currentUser && comment.author.id.equals(currentUser._id)) { %>
                                <a href="/campgrounds/<%= campground.slug %>/comments/<%= comment._id %>/edit" class="btn btn-sm btn-warning float-right">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <form action="/campgrounds/<%= campground.slug %>/comments/<%= comment._id %>?_method=DELETE" method="POST" class="deleteForm">
                                    <button class="btn btn-sm btn-danger float-right mr-2"><i class="fas fa-trash-alt"></i></button>    
                                </form>
                            <% } %>
                            <span class="float-right small mx-2"><%= moment(comment.createdAt).fromNow() %></span>
                            <p><%= comment.text %></p>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>
</div>

<% include ../partials/footer %>